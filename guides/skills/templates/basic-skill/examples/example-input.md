# Example Input

Replace with a representative input that exercises the skill's procedure.

The model uses examples as a strong signal for output formatting and style.
Pair every input file with a matching `example-output.md` showing the
expected result.
