# Example Output

Replace with the expected output for the matching `example-input.md`.

Make this a faithful representation of what the skill should produce: the
structure, the tone, the level of detail. The model will mimic it.
